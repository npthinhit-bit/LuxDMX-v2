#pragma once

/*
 * Marker header for the firmware-visible lux_test component.
 * Host and Unity tests are compiled from test/ and are not linked into the
 * production application; this header keeps the ESP-IDF component scaffold
 * valid for all PlatformIO environments.
 */
